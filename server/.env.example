# Comma-separated allowed frontend origins (local + production)
CLIENT_URLS=http://localhost:3000,http://127.0.0.1:3000,https://your-app.vercel.app
PORT=3001
# Random salt for IP hashing (privacy-safe report blocklist)
IP_SALT=change-this-random-string
